---
- name: Bootstrap access
  hosts: all
  become: true

  roles:
    - users


- name: Configure server
  hosts: all
  become: true
  remote_user: deploy

  roles:
    - common
    - docker
    - nginx
    - portainer